import '@/styles/globals.css';

/**
 * Custom App component for Next.js
 * Uses the latest Next.js 13+ app router recommended approach
 */
export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}